//Direkt kod till startsidan.
//funktioner som ska vara på startsidan anropaas i en funktion.
function startPage () {
  buildHeaderImage()
  addAdlibris()
  buildFooter()
  addStorytel()
}
startPage()
